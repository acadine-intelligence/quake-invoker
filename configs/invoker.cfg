// SPDX-License-Identifier: GPL-2.0-or-later
// Opt-in Invoker controls. The W key keeps its movement binding.
// Physical keys Q/E/R select orb types Q/W/E. F invokes the held combo.
bind q "orb q"
bind e "orb w"
bind r "orb e"
bind f "invoke"
seta cg_invokeEffects 1
